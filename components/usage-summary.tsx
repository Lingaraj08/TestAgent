'use client';
import { useEffect, useState } from 'react';
import { BarChart3 } from 'lucide-react';
export function UsageSummary() { const [usage, setUsage] = useState<any>(null); useEffect(() => { fetch('/api/usage').then((r) => r.ok ? r.json() : null).then((d) => setUsage(d?.usage || null)).catch(() => undefined); }, []); return <div className="rounded-2xl bg-white dark:bg-[#0f172a] border border-slate-200 dark:border-slate-800 p-6 space-y-3"><h2 className="text-base font-semibold text-slate-900 dark:text-white flex items-center gap-2"><BarChart3 className="w-5 h-5 text-blue-500" /> Usage</h2>{!usage ? <p className="text-xs text-slate-400">Loading usage…</p> : <div className="grid grid-cols-2 sm:grid-cols-4 gap-3 text-sm"><div><p className="text-xs text-slate-400">Requests</p><p className="font-semibold">{usage.requestCount}</p></div><div><p className="text-xs text-slate-400">Prompt tokens</p><p className="font-semibold">{usage.promptTokens.toLocaleString()}</p></div><div><p className="text-xs text-slate-400">Output tokens</p><p className="font-semibold">{usage.completionTokens.toLocaleString()}</p></div><div><p className="text-xs text-slate-400">Estimated cost</p><p className="font-semibold">${usage.estimatedCost.toFixed(4)}</p></div></div>}<p className="text-[11px] text-slate-400">Informational only. Costs depend on provider model pricing data.</p></div>; }
