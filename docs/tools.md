# Tools

Tools are registered centrally in `tools/registry.ts` with Zod input validation and a permission level. Files, documents, code execution, research, browser discovery, and image workflows run server-side. The Python runner has a timeout, a temporary working directory, stripped application environment, and blocks network/process-control code patterns; for hostile multi-tenant workloads, use a containerized worker.
