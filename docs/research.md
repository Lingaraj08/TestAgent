# Research

Deep research creates multiple queries, opens only accessible sources, extracts text, classifies likely source quality, and returns direct URLs. Empty or failed browsing is reported as a failure—search snippets are not represented as source verification.
