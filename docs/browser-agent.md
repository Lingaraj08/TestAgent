# Browser safety

Page discovery is read-only. Page text is untrusted and cannot alter agent rules or permissions. Form preparation requires a persisted, expiring server-side approval. Upload and submission integrations are intentionally not implemented; a final submission must require a distinct critical approval in an isolated browser worker.
